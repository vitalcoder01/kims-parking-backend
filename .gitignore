node_modules/
.env
.env.development
.env.production
.env.local
.env.test
dist/
*.log
npm-debug.log*
.DS_Store
prisma/migrations/**/migration.sql.bak

# One-time go-live seed script — contains the real admin password in plaintext.
prisma/seed-production.js

# Firebase service account key (FCM push) — real credential, never commit.
secrets/
