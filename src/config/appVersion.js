// Bumped by hand alongside each APK release (android/app/build.gradle
// versionCode/versionName) so the app can prompt users to update without
// needing its own release to know about the new release.
module.exports = {
  latestVersionCode: 25,
  latestVersionName: '1.8.1',
  apkUrl: 'https://raw.githubusercontent.com/vitalcoder01/kims-parking-frontend/main/releases/KIMS-Parking-v1.8.1.apk',
  notes: 'The update screen now shows the actual error if an in-app download fails, instead of a generic message.',
};
