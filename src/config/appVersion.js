// Bumped by hand alongside each APK release (android/app/build.gradle
// versionCode/versionName) so the app can prompt users to update without
// needing its own release to know about the new release.
module.exports = {
  latestVersionCode: 22,
  latestVersionName: '1.7.8',
  apkUrl: 'https://raw.githubusercontent.com/vitalcoder01/kims-parking-frontend/main/releases/KIMS-Parking-v1.7.8.apk',
  notes: 'Fixed several buttons (Confirm handover, Assign driver, and others) needing multiple taps, and staff parking records now update live instead of only on tab switch.',
};
