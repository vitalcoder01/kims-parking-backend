// Bumped by hand alongside each APK release (android/app/build.gradle
// versionCode/versionName) so the app can prompt users to update without
// needing its own release to know about the new release.
module.exports = {
  latestVersionCode: 26,
  latestVersionName: '1.8.2',
  apkUrl: 'https://raw.githubusercontent.com/vitalcoder01/kims-parking-frontend/main/releases/KIMS-Parking-v1.8.2.apk',
  notes: 'In-app update download and install is now actually fixed — future updates should install directly without going through the browser.',
};
